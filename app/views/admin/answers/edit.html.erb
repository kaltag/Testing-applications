<h1><%= t('.header') %></h1>

<%= render 'form', answer: @answer %>

<%= link_to t('.show_answer_button'), admin_answer_path(@answer) %> |
<%= link_to t('.back_to_question'), admin_question_path(id: @answer.question.id) %>
